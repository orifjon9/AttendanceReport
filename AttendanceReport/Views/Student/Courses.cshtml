@using AttendanceReport.Models
@model List<EnrollmentViewModel>
@{
    ViewBag.Title = "Student Courses";
}

<h2>@ViewBag.Title</h2>
<div class="row">
    @if (Model != null)
    {
        <div class="col-sm-12">
            @Html.Partial("~/Views/Contents/EnrollmentCourses.cshtml", Model)
        </div>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

}